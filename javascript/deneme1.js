var f1 = function validate(results)
    {


    for (index = 0; index < results.length; index++) {
    }         

    var innerFunction = function (apartment)
    {
        alert(index);

    }

    var retObj  = {"innerFunction":innerFunction};
    return retObj;

}

